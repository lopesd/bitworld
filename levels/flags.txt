Two values separated by a colon indicated a key-value pair.
Items separated only by commas indicate items in a list, which must be correctly ordered.


-- OUTDATED INFO --
MAPS: 
	n : name
	t : tag
	L() : links to other maps
	-F : first level

WALLS:

BITS:
	M((x,y),(w,z)) : Movement(vector, vector) [not required, defaults to no movement]
	-r : resistant to corruption

DOORS:
	t : tag
	D(m: d: ) : Destination(map door)
	w : weight
	
PULSER:
	r : radius
	P(w: p:) : Period(wait: pulse:)

WHITE BIT:
	-p : phases
	s : speed 
